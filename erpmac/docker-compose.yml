version: "3.8"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: erpmac
    ports:
      - "5432:5432"
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    depends_on:
      - db
